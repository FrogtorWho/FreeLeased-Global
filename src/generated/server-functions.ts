// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  SignoffType,
  SignoffCreateInput,
  SignoffUpdateInput,
  AuditEntryType,
  AuditEntryCreateInput,
  AuditEntryUpdateInput,
  ResearchTaskType,
  ResearchTaskCreateInput,
  ResearchTaskUpdateInput,
  JurisdictionRecordType,
  JurisdictionRecordCreateInput,
  JurisdictionRecordUpdateInput,
  StatuteRecordType,
  StatuteRecordCreateInput,
  StatuteRecordUpdateInput,
  CompetitionTaskType,
  CompetitionTaskCreateInput,
  CompetitionTaskUpdateInput,
  ContentDraftType,
  ContentDraftCreateInput,
  ContentDraftUpdateInput,
  ContractRecordType,
  ContractRecordCreateInput,
  ContractRecordUpdateInput,
  ResidentGroupType,
  ResidentGroupCreateInput,
  ResidentGroupUpdateInput,
  GroupMemberType,
  GroupMemberCreateInput,
  GroupMemberUpdateInput,
  GroupTaskType,
  GroupTaskCreateInput,
  GroupTaskUpdateInput,
  GroupVoteType,
  GroupVoteCreateInput,
  GroupVoteUpdateInput,
  GroupBallotType,
  GroupBallotCreateInput,
  GroupBallotUpdateInput,
  GroupMessageType,
  GroupMessageCreateInput,
  GroupMessageUpdateInput,
  GroupDocumentType,
  GroupDocumentCreateInput,
  GroupDocumentUpdateInput,
  GroupMilestoneType,
  GroupMilestoneCreateInput,
  GroupMilestoneUpdateInput,
  CaptureSessionType,
  CaptureSessionCreateInput,
  CaptureSessionUpdateInput,
  CorrespondenceTemplateType,
  CorrespondenceTemplateCreateInput,
  CorrespondenceTemplateUpdateInput,
  SignedDocumentType,
  SignedDocumentCreateInput,
  SignedDocumentUpdateInput,
  DocumentSignatureType,
  DocumentSignatureCreateInput,
  DocumentSignatureUpdateInput,
  ReviewItemType,
  ReviewItemCreateInput,
  ReviewItemUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// Signoff Client Functions
// ============================================================================

/**
 * List all Signoff records
 */
export async function getSignoffList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<SignoffType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/signoffs${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Signoff')
  }
  const json = await response.json()
  return (json.items || []) as SignoffType[]
}

/**
 * Get a single Signoff by ID
 */
export async function getSignoffById(args: { data: { id: string; userId?: string } }): Promise<SignoffType> {
  const url = `${getApiBase()}/api/signoffs/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Signoff not found')
  }
  const json = await response.json()
  return json.data as SignoffType
}

/**
 * Create a new Signoff
 */
export async function createSignoff(args: { data: { input: SignoffCreateInput; userId?: string } }): Promise<SignoffType> {
  const url = `${getApiBase()}/api/signoffs`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Signoff')
  }
  const json = await response.json()
  return json.data as SignoffType
}

/**
 * Update an existing Signoff
 */
export async function updateSignoff(args: { data: { id: string; input: SignoffUpdateInput; userId?: string } }): Promise<SignoffType> {
  const url = `${getApiBase()}/api/signoffs/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Signoff')
  }
  const json = await response.json()
  return json.data as SignoffType
}

/**
 * Delete a Signoff
 */
export async function deleteSignoff(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/signoffs/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Signoff')
  }
  return { success: true }
}

// ============================================================================
// AuditEntry Client Functions
// ============================================================================

/**
 * List all AuditEntry records
 */
export async function getAuditEntryList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<AuditEntryType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/audit-entries${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list AuditEntry')
  }
  const json = await response.json()
  return (json.items || []) as AuditEntryType[]
}

/**
 * Get a single AuditEntry by ID
 */
export async function getAuditEntryById(args: { data: { id: string; userId?: string } }): Promise<AuditEntryType> {
  const url = `${getApiBase()}/api/audit-entries/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'AuditEntry not found')
  }
  const json = await response.json()
  return json.data as AuditEntryType
}

/**
 * Create a new AuditEntry
 */
export async function createAuditEntry(args: { data: { input: AuditEntryCreateInput; userId?: string } }): Promise<AuditEntryType> {
  const url = `${getApiBase()}/api/audit-entries`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create AuditEntry')
  }
  const json = await response.json()
  return json.data as AuditEntryType
}

/**
 * Update an existing AuditEntry
 */
export async function updateAuditEntry(args: { data: { id: string; input: AuditEntryUpdateInput; userId?: string } }): Promise<AuditEntryType> {
  const url = `${getApiBase()}/api/audit-entries/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update AuditEntry')
  }
  const json = await response.json()
  return json.data as AuditEntryType
}

/**
 * Delete a AuditEntry
 */
export async function deleteAuditEntry(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/audit-entries/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete AuditEntry')
  }
  return { success: true }
}

// ============================================================================
// ResearchTask Client Functions
// ============================================================================

/**
 * List all ResearchTask records
 */
export async function getResearchTaskList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ResearchTaskType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/research-tasks${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ResearchTask')
  }
  const json = await response.json()
  return (json.items || []) as ResearchTaskType[]
}

/**
 * Get a single ResearchTask by ID
 */
export async function getResearchTaskById(args: { data: { id: string; userId?: string } }): Promise<ResearchTaskType> {
  const url = `${getApiBase()}/api/research-tasks/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ResearchTask not found')
  }
  const json = await response.json()
  return json.data as ResearchTaskType
}

/**
 * Create a new ResearchTask
 */
export async function createResearchTask(args: { data: { input: ResearchTaskCreateInput; userId?: string } }): Promise<ResearchTaskType> {
  const url = `${getApiBase()}/api/research-tasks`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ResearchTask')
  }
  const json = await response.json()
  return json.data as ResearchTaskType
}

/**
 * Update an existing ResearchTask
 */
export async function updateResearchTask(args: { data: { id: string; input: ResearchTaskUpdateInput; userId?: string } }): Promise<ResearchTaskType> {
  const url = `${getApiBase()}/api/research-tasks/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ResearchTask')
  }
  const json = await response.json()
  return json.data as ResearchTaskType
}

/**
 * Delete a ResearchTask
 */
export async function deleteResearchTask(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/research-tasks/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ResearchTask')
  }
  return { success: true }
}

// ============================================================================
// JurisdictionRecord Client Functions
// ============================================================================

/**
 * List all JurisdictionRecord records
 */
export async function getJurisdictionRecordList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<JurisdictionRecordType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/jurisdiction-records${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list JurisdictionRecord')
  }
  const json = await response.json()
  return (json.items || []) as JurisdictionRecordType[]
}

/**
 * Get a single JurisdictionRecord by ID
 */
export async function getJurisdictionRecordById(args: { data: { id: string; userId?: string } }): Promise<JurisdictionRecordType> {
  const url = `${getApiBase()}/api/jurisdiction-records/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'JurisdictionRecord not found')
  }
  const json = await response.json()
  return json.data as JurisdictionRecordType
}

/**
 * Create a new JurisdictionRecord
 */
export async function createJurisdictionRecord(args: { data: { input: JurisdictionRecordCreateInput; userId?: string } }): Promise<JurisdictionRecordType> {
  const url = `${getApiBase()}/api/jurisdiction-records`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create JurisdictionRecord')
  }
  const json = await response.json()
  return json.data as JurisdictionRecordType
}

/**
 * Update an existing JurisdictionRecord
 */
export async function updateJurisdictionRecord(args: { data: { id: string; input: JurisdictionRecordUpdateInput; userId?: string } }): Promise<JurisdictionRecordType> {
  const url = `${getApiBase()}/api/jurisdiction-records/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update JurisdictionRecord')
  }
  const json = await response.json()
  return json.data as JurisdictionRecordType
}

/**
 * Delete a JurisdictionRecord
 */
export async function deleteJurisdictionRecord(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/jurisdiction-records/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete JurisdictionRecord')
  }
  return { success: true }
}

// ============================================================================
// StatuteRecord Client Functions
// ============================================================================

/**
 * List all StatuteRecord records
 */
export async function getStatuteRecordList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<StatuteRecordType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/statute-records${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list StatuteRecord')
  }
  const json = await response.json()
  return (json.items || []) as StatuteRecordType[]
}

/**
 * Get a single StatuteRecord by ID
 */
export async function getStatuteRecordById(args: { data: { id: string; userId?: string } }): Promise<StatuteRecordType> {
  const url = `${getApiBase()}/api/statute-records/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'StatuteRecord not found')
  }
  const json = await response.json()
  return json.data as StatuteRecordType
}

/**
 * Create a new StatuteRecord
 */
export async function createStatuteRecord(args: { data: { input: StatuteRecordCreateInput; userId?: string } }): Promise<StatuteRecordType> {
  const url = `${getApiBase()}/api/statute-records`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create StatuteRecord')
  }
  const json = await response.json()
  return json.data as StatuteRecordType
}

/**
 * Update an existing StatuteRecord
 */
export async function updateStatuteRecord(args: { data: { id: string; input: StatuteRecordUpdateInput; userId?: string } }): Promise<StatuteRecordType> {
  const url = `${getApiBase()}/api/statute-records/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update StatuteRecord')
  }
  const json = await response.json()
  return json.data as StatuteRecordType
}

/**
 * Delete a StatuteRecord
 */
export async function deleteStatuteRecord(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/statute-records/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete StatuteRecord')
  }
  return { success: true }
}

// ============================================================================
// CompetitionTask Client Functions
// ============================================================================

/**
 * List all CompetitionTask records
 */
export async function getCompetitionTaskList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<CompetitionTaskType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/competition-tasks${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list CompetitionTask')
  }
  const json = await response.json()
  return (json.items || []) as CompetitionTaskType[]
}

/**
 * Get a single CompetitionTask by ID
 */
export async function getCompetitionTaskById(args: { data: { id: string; userId?: string } }): Promise<CompetitionTaskType> {
  const url = `${getApiBase()}/api/competition-tasks/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'CompetitionTask not found')
  }
  const json = await response.json()
  return json.data as CompetitionTaskType
}

/**
 * Create a new CompetitionTask
 */
export async function createCompetitionTask(args: { data: { input: CompetitionTaskCreateInput; userId?: string } }): Promise<CompetitionTaskType> {
  const url = `${getApiBase()}/api/competition-tasks`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create CompetitionTask')
  }
  const json = await response.json()
  return json.data as CompetitionTaskType
}

/**
 * Update an existing CompetitionTask
 */
export async function updateCompetitionTask(args: { data: { id: string; input: CompetitionTaskUpdateInput; userId?: string } }): Promise<CompetitionTaskType> {
  const url = `${getApiBase()}/api/competition-tasks/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update CompetitionTask')
  }
  const json = await response.json()
  return json.data as CompetitionTaskType
}

/**
 * Delete a CompetitionTask
 */
export async function deleteCompetitionTask(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/competition-tasks/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete CompetitionTask')
  }
  return { success: true }
}

// ============================================================================
// ContentDraft Client Functions
// ============================================================================

/**
 * List all ContentDraft records
 */
export async function getContentDraftList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ContentDraftType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/content-drafts${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ContentDraft')
  }
  const json = await response.json()
  return (json.items || []) as ContentDraftType[]
}

/**
 * Get a single ContentDraft by ID
 */
export async function getContentDraftById(args: { data: { id: string; userId?: string } }): Promise<ContentDraftType> {
  const url = `${getApiBase()}/api/content-drafts/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ContentDraft not found')
  }
  const json = await response.json()
  return json.data as ContentDraftType
}

/**
 * Create a new ContentDraft
 */
export async function createContentDraft(args: { data: { input: ContentDraftCreateInput; userId?: string } }): Promise<ContentDraftType> {
  const url = `${getApiBase()}/api/content-drafts`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ContentDraft')
  }
  const json = await response.json()
  return json.data as ContentDraftType
}

/**
 * Update an existing ContentDraft
 */
export async function updateContentDraft(args: { data: { id: string; input: ContentDraftUpdateInput; userId?: string } }): Promise<ContentDraftType> {
  const url = `${getApiBase()}/api/content-drafts/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ContentDraft')
  }
  const json = await response.json()
  return json.data as ContentDraftType
}

/**
 * Delete a ContentDraft
 */
export async function deleteContentDraft(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/content-drafts/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ContentDraft')
  }
  return { success: true }
}

// ============================================================================
// ContractRecord Client Functions
// ============================================================================

/**
 * List all ContractRecord records
 */
export async function getContractRecordList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ContractRecordType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/contract-records${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ContractRecord')
  }
  const json = await response.json()
  return (json.items || []) as ContractRecordType[]
}

/**
 * Get a single ContractRecord by ID
 */
export async function getContractRecordById(args: { data: { id: string; userId?: string } }): Promise<ContractRecordType> {
  const url = `${getApiBase()}/api/contract-records/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ContractRecord not found')
  }
  const json = await response.json()
  return json.data as ContractRecordType
}

/**
 * Create a new ContractRecord
 */
export async function createContractRecord(args: { data: { input: ContractRecordCreateInput; userId?: string } }): Promise<ContractRecordType> {
  const url = `${getApiBase()}/api/contract-records`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ContractRecord')
  }
  const json = await response.json()
  return json.data as ContractRecordType
}

/**
 * Update an existing ContractRecord
 */
export async function updateContractRecord(args: { data: { id: string; input: ContractRecordUpdateInput; userId?: string } }): Promise<ContractRecordType> {
  const url = `${getApiBase()}/api/contract-records/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ContractRecord')
  }
  const json = await response.json()
  return json.data as ContractRecordType
}

/**
 * Delete a ContractRecord
 */
export async function deleteContractRecord(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/contract-records/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ContractRecord')
  }
  return { success: true }
}

// ============================================================================
// ResidentGroup Client Functions
// ============================================================================

/**
 * List all ResidentGroup records
 */
export async function getResidentGroupList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ResidentGroupType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/resident-groups${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ResidentGroup')
  }
  const json = await response.json()
  return (json.items || []) as ResidentGroupType[]
}

/**
 * Get a single ResidentGroup by ID
 */
export async function getResidentGroupById(args: { data: { id: string; userId?: string } }): Promise<ResidentGroupType> {
  const url = `${getApiBase()}/api/resident-groups/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ResidentGroup not found')
  }
  const json = await response.json()
  return json.data as ResidentGroupType
}

/**
 * Create a new ResidentGroup
 */
export async function createResidentGroup(args: { data: { input: ResidentGroupCreateInput; userId?: string } }): Promise<ResidentGroupType> {
  const url = `${getApiBase()}/api/resident-groups`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ResidentGroup')
  }
  const json = await response.json()
  return json.data as ResidentGroupType
}

/**
 * Update an existing ResidentGroup
 */
export async function updateResidentGroup(args: { data: { id: string; input: ResidentGroupUpdateInput; userId?: string } }): Promise<ResidentGroupType> {
  const url = `${getApiBase()}/api/resident-groups/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ResidentGroup')
  }
  const json = await response.json()
  return json.data as ResidentGroupType
}

/**
 * Delete a ResidentGroup
 */
export async function deleteResidentGroup(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/resident-groups/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ResidentGroup')
  }
  return { success: true }
}

// ============================================================================
// GroupMember Client Functions
// ============================================================================

/**
 * List all GroupMember records
 */
export async function getGroupMemberList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<GroupMemberType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/group-members${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list GroupMember')
  }
  const json = await response.json()
  return (json.items || []) as GroupMemberType[]
}

/**
 * Get a single GroupMember by ID
 */
export async function getGroupMemberById(args: { data: { id: string; userId?: string } }): Promise<GroupMemberType> {
  const url = `${getApiBase()}/api/group-members/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'GroupMember not found')
  }
  const json = await response.json()
  return json.data as GroupMemberType
}

/**
 * Create a new GroupMember
 */
export async function createGroupMember(args: { data: { input: GroupMemberCreateInput; userId?: string } }): Promise<GroupMemberType> {
  const url = `${getApiBase()}/api/group-members`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create GroupMember')
  }
  const json = await response.json()
  return json.data as GroupMemberType
}

/**
 * Update an existing GroupMember
 */
export async function updateGroupMember(args: { data: { id: string; input: GroupMemberUpdateInput; userId?: string } }): Promise<GroupMemberType> {
  const url = `${getApiBase()}/api/group-members/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update GroupMember')
  }
  const json = await response.json()
  return json.data as GroupMemberType
}

/**
 * Delete a GroupMember
 */
export async function deleteGroupMember(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/group-members/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete GroupMember')
  }
  return { success: true }
}

// ============================================================================
// GroupTask Client Functions
// ============================================================================

/**
 * List all GroupTask records
 */
export async function getGroupTaskList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<GroupTaskType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/group-tasks${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list GroupTask')
  }
  const json = await response.json()
  return (json.items || []) as GroupTaskType[]
}

/**
 * Get a single GroupTask by ID
 */
export async function getGroupTaskById(args: { data: { id: string; userId?: string } }): Promise<GroupTaskType> {
  const url = `${getApiBase()}/api/group-tasks/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'GroupTask not found')
  }
  const json = await response.json()
  return json.data as GroupTaskType
}

/**
 * Create a new GroupTask
 */
export async function createGroupTask(args: { data: { input: GroupTaskCreateInput; userId?: string } }): Promise<GroupTaskType> {
  const url = `${getApiBase()}/api/group-tasks`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create GroupTask')
  }
  const json = await response.json()
  return json.data as GroupTaskType
}

/**
 * Update an existing GroupTask
 */
export async function updateGroupTask(args: { data: { id: string; input: GroupTaskUpdateInput; userId?: string } }): Promise<GroupTaskType> {
  const url = `${getApiBase()}/api/group-tasks/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update GroupTask')
  }
  const json = await response.json()
  return json.data as GroupTaskType
}

/**
 * Delete a GroupTask
 */
export async function deleteGroupTask(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/group-tasks/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete GroupTask')
  }
  return { success: true }
}

// ============================================================================
// GroupVote Client Functions
// ============================================================================

/**
 * List all GroupVote records
 */
export async function getGroupVoteList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<GroupVoteType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/group-votes${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list GroupVote')
  }
  const json = await response.json()
  return (json.items || []) as GroupVoteType[]
}

/**
 * Get a single GroupVote by ID
 */
export async function getGroupVoteById(args: { data: { id: string; userId?: string } }): Promise<GroupVoteType> {
  const url = `${getApiBase()}/api/group-votes/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'GroupVote not found')
  }
  const json = await response.json()
  return json.data as GroupVoteType
}

/**
 * Create a new GroupVote
 */
export async function createGroupVote(args: { data: { input: GroupVoteCreateInput; userId?: string } }): Promise<GroupVoteType> {
  const url = `${getApiBase()}/api/group-votes`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create GroupVote')
  }
  const json = await response.json()
  return json.data as GroupVoteType
}

/**
 * Update an existing GroupVote
 */
export async function updateGroupVote(args: { data: { id: string; input: GroupVoteUpdateInput; userId?: string } }): Promise<GroupVoteType> {
  const url = `${getApiBase()}/api/group-votes/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update GroupVote')
  }
  const json = await response.json()
  return json.data as GroupVoteType
}

/**
 * Delete a GroupVote
 */
export async function deleteGroupVote(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/group-votes/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete GroupVote')
  }
  return { success: true }
}

// ============================================================================
// GroupBallot Client Functions
// ============================================================================

/**
 * List all GroupBallot records
 */
export async function getGroupBallotList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<GroupBallotType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/group-ballots${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list GroupBallot')
  }
  const json = await response.json()
  return (json.items || []) as GroupBallotType[]
}

/**
 * Get a single GroupBallot by ID
 */
export async function getGroupBallotById(args: { data: { id: string; userId?: string } }): Promise<GroupBallotType> {
  const url = `${getApiBase()}/api/group-ballots/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'GroupBallot not found')
  }
  const json = await response.json()
  return json.data as GroupBallotType
}

/**
 * Create a new GroupBallot
 */
export async function createGroupBallot(args: { data: { input: GroupBallotCreateInput; userId?: string } }): Promise<GroupBallotType> {
  const url = `${getApiBase()}/api/group-ballots`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create GroupBallot')
  }
  const json = await response.json()
  return json.data as GroupBallotType
}

/**
 * Update an existing GroupBallot
 */
export async function updateGroupBallot(args: { data: { id: string; input: GroupBallotUpdateInput; userId?: string } }): Promise<GroupBallotType> {
  const url = `${getApiBase()}/api/group-ballots/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update GroupBallot')
  }
  const json = await response.json()
  return json.data as GroupBallotType
}

/**
 * Delete a GroupBallot
 */
export async function deleteGroupBallot(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/group-ballots/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete GroupBallot')
  }
  return { success: true }
}

// ============================================================================
// GroupMessage Client Functions
// ============================================================================

/**
 * List all GroupMessage records
 */
export async function getGroupMessageList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<GroupMessageType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/group-messages${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list GroupMessage')
  }
  const json = await response.json()
  return (json.items || []) as GroupMessageType[]
}

/**
 * Get a single GroupMessage by ID
 */
export async function getGroupMessageById(args: { data: { id: string; userId?: string } }): Promise<GroupMessageType> {
  const url = `${getApiBase()}/api/group-messages/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'GroupMessage not found')
  }
  const json = await response.json()
  return json.data as GroupMessageType
}

/**
 * Create a new GroupMessage
 */
export async function createGroupMessage(args: { data: { input: GroupMessageCreateInput; userId?: string } }): Promise<GroupMessageType> {
  const url = `${getApiBase()}/api/group-messages`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create GroupMessage')
  }
  const json = await response.json()
  return json.data as GroupMessageType
}

/**
 * Update an existing GroupMessage
 */
export async function updateGroupMessage(args: { data: { id: string; input: GroupMessageUpdateInput; userId?: string } }): Promise<GroupMessageType> {
  const url = `${getApiBase()}/api/group-messages/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update GroupMessage')
  }
  const json = await response.json()
  return json.data as GroupMessageType
}

/**
 * Delete a GroupMessage
 */
export async function deleteGroupMessage(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/group-messages/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete GroupMessage')
  }
  return { success: true }
}

// ============================================================================
// GroupDocument Client Functions
// ============================================================================

/**
 * List all GroupDocument records
 */
export async function getGroupDocumentList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<GroupDocumentType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/group-documents${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list GroupDocument')
  }
  const json = await response.json()
  return (json.items || []) as GroupDocumentType[]
}

/**
 * Get a single GroupDocument by ID
 */
export async function getGroupDocumentById(args: { data: { id: string; userId?: string } }): Promise<GroupDocumentType> {
  const url = `${getApiBase()}/api/group-documents/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'GroupDocument not found')
  }
  const json = await response.json()
  return json.data as GroupDocumentType
}

/**
 * Create a new GroupDocument
 */
export async function createGroupDocument(args: { data: { input: GroupDocumentCreateInput; userId?: string } }): Promise<GroupDocumentType> {
  const url = `${getApiBase()}/api/group-documents`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create GroupDocument')
  }
  const json = await response.json()
  return json.data as GroupDocumentType
}

/**
 * Update an existing GroupDocument
 */
export async function updateGroupDocument(args: { data: { id: string; input: GroupDocumentUpdateInput; userId?: string } }): Promise<GroupDocumentType> {
  const url = `${getApiBase()}/api/group-documents/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update GroupDocument')
  }
  const json = await response.json()
  return json.data as GroupDocumentType
}

/**
 * Delete a GroupDocument
 */
export async function deleteGroupDocument(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/group-documents/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete GroupDocument')
  }
  return { success: true }
}

// ============================================================================
// GroupMilestone Client Functions
// ============================================================================

/**
 * List all GroupMilestone records
 */
export async function getGroupMilestoneList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<GroupMilestoneType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/group-milestones${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list GroupMilestone')
  }
  const json = await response.json()
  return (json.items || []) as GroupMilestoneType[]
}

/**
 * Get a single GroupMilestone by ID
 */
export async function getGroupMilestoneById(args: { data: { id: string; userId?: string } }): Promise<GroupMilestoneType> {
  const url = `${getApiBase()}/api/group-milestones/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'GroupMilestone not found')
  }
  const json = await response.json()
  return json.data as GroupMilestoneType
}

/**
 * Create a new GroupMilestone
 */
export async function createGroupMilestone(args: { data: { input: GroupMilestoneCreateInput; userId?: string } }): Promise<GroupMilestoneType> {
  const url = `${getApiBase()}/api/group-milestones`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create GroupMilestone')
  }
  const json = await response.json()
  return json.data as GroupMilestoneType
}

/**
 * Update an existing GroupMilestone
 */
export async function updateGroupMilestone(args: { data: { id: string; input: GroupMilestoneUpdateInput; userId?: string } }): Promise<GroupMilestoneType> {
  const url = `${getApiBase()}/api/group-milestones/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update GroupMilestone')
  }
  const json = await response.json()
  return json.data as GroupMilestoneType
}

/**
 * Delete a GroupMilestone
 */
export async function deleteGroupMilestone(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/group-milestones/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete GroupMilestone')
  }
  return { success: true }
}

// ============================================================================
// CaptureSession Client Functions
// ============================================================================

/**
 * List all CaptureSession records
 */
export async function getCaptureSessionList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<CaptureSessionType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/capture-sessions${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list CaptureSession')
  }
  const json = await response.json()
  return (json.items || []) as CaptureSessionType[]
}

/**
 * Get a single CaptureSession by ID
 */
export async function getCaptureSessionById(args: { data: { id: string; userId?: string } }): Promise<CaptureSessionType> {
  const url = `${getApiBase()}/api/capture-sessions/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'CaptureSession not found')
  }
  const json = await response.json()
  return json.data as CaptureSessionType
}

/**
 * Create a new CaptureSession
 */
export async function createCaptureSession(args: { data: { input: CaptureSessionCreateInput; userId?: string } }): Promise<CaptureSessionType> {
  const url = `${getApiBase()}/api/capture-sessions`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create CaptureSession')
  }
  const json = await response.json()
  return json.data as CaptureSessionType
}

/**
 * Update an existing CaptureSession
 */
export async function updateCaptureSession(args: { data: { id: string; input: CaptureSessionUpdateInput; userId?: string } }): Promise<CaptureSessionType> {
  const url = `${getApiBase()}/api/capture-sessions/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update CaptureSession')
  }
  const json = await response.json()
  return json.data as CaptureSessionType
}

/**
 * Delete a CaptureSession
 */
export async function deleteCaptureSession(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/capture-sessions/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete CaptureSession')
  }
  return { success: true }
}

// ============================================================================
// CorrespondenceTemplate Client Functions
// ============================================================================

/**
 * List all CorrespondenceTemplate records
 */
export async function getCorrespondenceTemplateList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<CorrespondenceTemplateType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/correspondence-templates${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list CorrespondenceTemplate')
  }
  const json = await response.json()
  return (json.items || []) as CorrespondenceTemplateType[]
}

/**
 * Get a single CorrespondenceTemplate by ID
 */
export async function getCorrespondenceTemplateById(args: { data: { id: string; userId?: string } }): Promise<CorrespondenceTemplateType> {
  const url = `${getApiBase()}/api/correspondence-templates/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'CorrespondenceTemplate not found')
  }
  const json = await response.json()
  return json.data as CorrespondenceTemplateType
}

/**
 * Create a new CorrespondenceTemplate
 */
export async function createCorrespondenceTemplate(args: { data: { input: CorrespondenceTemplateCreateInput; userId?: string } }): Promise<CorrespondenceTemplateType> {
  const url = `${getApiBase()}/api/correspondence-templates`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create CorrespondenceTemplate')
  }
  const json = await response.json()
  return json.data as CorrespondenceTemplateType
}

/**
 * Update an existing CorrespondenceTemplate
 */
export async function updateCorrespondenceTemplate(args: { data: { id: string; input: CorrespondenceTemplateUpdateInput; userId?: string } }): Promise<CorrespondenceTemplateType> {
  const url = `${getApiBase()}/api/correspondence-templates/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update CorrespondenceTemplate')
  }
  const json = await response.json()
  return json.data as CorrespondenceTemplateType
}

/**
 * Delete a CorrespondenceTemplate
 */
export async function deleteCorrespondenceTemplate(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/correspondence-templates/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete CorrespondenceTemplate')
  }
  return { success: true }
}

// ============================================================================
// SignedDocument Client Functions
// ============================================================================

/**
 * List all SignedDocument records
 */
export async function getSignedDocumentList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<SignedDocumentType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/signed-documents${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list SignedDocument')
  }
  const json = await response.json()
  return (json.items || []) as SignedDocumentType[]
}

/**
 * Get a single SignedDocument by ID
 */
export async function getSignedDocumentById(args: { data: { id: string; userId?: string } }): Promise<SignedDocumentType> {
  const url = `${getApiBase()}/api/signed-documents/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'SignedDocument not found')
  }
  const json = await response.json()
  return json.data as SignedDocumentType
}

/**
 * Create a new SignedDocument
 */
export async function createSignedDocument(args: { data: { input: SignedDocumentCreateInput; userId?: string } }): Promise<SignedDocumentType> {
  const url = `${getApiBase()}/api/signed-documents`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create SignedDocument')
  }
  const json = await response.json()
  return json.data as SignedDocumentType
}

/**
 * Update an existing SignedDocument
 */
export async function updateSignedDocument(args: { data: { id: string; input: SignedDocumentUpdateInput; userId?: string } }): Promise<SignedDocumentType> {
  const url = `${getApiBase()}/api/signed-documents/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update SignedDocument')
  }
  const json = await response.json()
  return json.data as SignedDocumentType
}

/**
 * Delete a SignedDocument
 */
export async function deleteSignedDocument(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/signed-documents/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete SignedDocument')
  }
  return { success: true }
}

// ============================================================================
// DocumentSignature Client Functions
// ============================================================================

/**
 * List all DocumentSignature records
 */
export async function getDocumentSignatureList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<DocumentSignatureType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/document-signatures${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list DocumentSignature')
  }
  const json = await response.json()
  return (json.items || []) as DocumentSignatureType[]
}

/**
 * Get a single DocumentSignature by ID
 */
export async function getDocumentSignatureById(args: { data: { id: string; userId?: string } }): Promise<DocumentSignatureType> {
  const url = `${getApiBase()}/api/document-signatures/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'DocumentSignature not found')
  }
  const json = await response.json()
  return json.data as DocumentSignatureType
}

/**
 * Create a new DocumentSignature
 */
export async function createDocumentSignature(args: { data: { input: DocumentSignatureCreateInput; userId?: string } }): Promise<DocumentSignatureType> {
  const url = `${getApiBase()}/api/document-signatures`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create DocumentSignature')
  }
  const json = await response.json()
  return json.data as DocumentSignatureType
}

/**
 * Update an existing DocumentSignature
 */
export async function updateDocumentSignature(args: { data: { id: string; input: DocumentSignatureUpdateInput; userId?: string } }): Promise<DocumentSignatureType> {
  const url = `${getApiBase()}/api/document-signatures/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update DocumentSignature')
  }
  const json = await response.json()
  return json.data as DocumentSignatureType
}

/**
 * Delete a DocumentSignature
 */
export async function deleteDocumentSignature(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/document-signatures/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete DocumentSignature')
  }
  return { success: true }
}

// ============================================================================
// ReviewItem Client Functions
// ============================================================================

/**
 * List all ReviewItem records
 */
export async function getReviewItemList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ReviewItemType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/review-items${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ReviewItem')
  }
  const json = await response.json()
  return (json.items || []) as ReviewItemType[]
}

/**
 * Get a single ReviewItem by ID
 */
export async function getReviewItemById(args: { data: { id: string; userId?: string } }): Promise<ReviewItemType> {
  const url = `${getApiBase()}/api/review-items/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ReviewItem not found')
  }
  const json = await response.json()
  return json.data as ReviewItemType
}

/**
 * Create a new ReviewItem
 */
export async function createReviewItem(args: { data: { input: ReviewItemCreateInput; userId?: string } }): Promise<ReviewItemType> {
  const url = `${getApiBase()}/api/review-items`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ReviewItem')
  }
  const json = await response.json()
  return json.data as ReviewItemType
}

/**
 * Update an existing ReviewItem
 */
export async function updateReviewItem(args: { data: { id: string; input: ReviewItemUpdateInput; userId?: string } }): Promise<ReviewItemType> {
  const url = `${getApiBase()}/api/review-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ReviewItem')
  }
  const json = await response.json()
  return json.data as ReviewItemType
}

/**
 * Delete a ReviewItem
 */
export async function deleteReviewItem(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/review-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ReviewItem')
  }
  return { success: true }
}
